mysql pod will be connected to svc pod with ClusterIp type
application pod with java code will be having svc type as LoadBalancer so users connect on external browser
application pod will be connected(sending request to) with the svc of the mysql pod.


